# flask example
## process
```
python -m venv .venv
source ./.venv/bin/activate
pip install flask
pip install flake8
flake8 --install-hook git
pip freeze > requirements.txt
vi .gitignore
# VSCode setting
```



